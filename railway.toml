[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "cd /app && python main.py"
healthcheckPath = "/health"
restartPolicyType = "on_failure"

# deploy-trigger-v5.11.0
